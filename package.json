{
  "name": "goodvibes",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run serve",
    "build": "cd web && yarn && yarn build && cd ../",
    "serve": "node server/app.js",
    "serve-dev": "nodemon server/app.js",
    "seed": "node server/app.js seed",
    "preinstall": "yarn build",
    "deploy": "git add . && git ci -m 'update' && git push origin master && git push heroku master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.2",
    "connect-mongo": "^2.0.2",
    "express": "^4.16.4",
    "express-session": "^1.15.6",
    "mongoose": "^5.3.9",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "async-request": "^1.2.0",
    "cheerio": "^1.0.0-rc.2",
    "image-downloader": "^3.4.1",
    "mkdirp": "^0.5.1"
  }
}
